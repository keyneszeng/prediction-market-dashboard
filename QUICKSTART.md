# 快速启动指南

## 第一步：安装依赖

在项目根目录运行：

```bash
npm run install:all
```

这将安装：
- 根目录的依赖（concurrently）
- 服务器端依赖（Express等）
- 客户端依赖（React、Vite等）

## 第二步：启动应用

### 方式一：同时启动前后端（推荐）

```bash
npm run dev
```

这将同时启动：
- 前端开发服务器：http://localhost:3000
- 后端API服务器：http://localhost:3001

### 方式二：分别启动

**终端1 - 启动后端：**
```bash
npm run dev:server
```

**终端2 - 启动前端：**
```bash
npm run dev:client
```

## 第三步：使用应用

1. 打开浏览器访问 http://localhost:3000
2. 等待数据加载完成
3. 在搜索框输入关键词（如"选举"、"比特币"等）搜索相关事件
4. 点击任意平台卡片跳转到该平台主页

## 功能说明

### 搜索功能
- 在顶部搜索框输入事件名称或关键词
- 系统会实时搜索所有平台的相关预测市场
- 支持中文和英文搜索

### 数据展示
- 每个平台卡片显示：
  - 平台名称和在线状态
  - 事件数量
  - 总交易量
  - 各事件的详细下注情况（是/否概率、交易量）

### 跳转功能
- 点击任意平台卡片即可在新标签页打开该平台主页

### 自动刷新
- 数据每30秒自动刷新一次
- 页面顶部显示最后更新时间

## 故障排除

### 问题：前端无法连接后端
- 确保后端服务器正在运行（http://localhost:3001）
- 检查后端控制台是否有错误信息

### 问题：数据加载失败
- 检查网络连接
- 查看浏览器控制台的错误信息
- 确认后端API端点正常工作（访问 http://localhost:3001/api/health 检查）

### 问题：依赖安装失败
- 确保已安装 Node.js (版本 16+)
- 尝试清除缓存：`npm cache clean --force`
- 删除 node_modules 文件夹后重新安装

## 下一步

要集成真实的API，请编辑 `server/services/marketServices.js` 文件，替换模拟数据为真实的API调用。


